# docker compose 用の環境変数。`cp .env.example .env` して使う（.env はコミットしない）。
# 開発用のダミー値。本番では別の値を安全に管理する。

# PostgreSQL
POSTGRES_DB=tidepo
POSTGRES_USER=tidepo
POSTGRES_PASSWORD=tidepo_dev_password
# ホスト側の公開ポート。ローカルで 5432 が使用中なら変更する（例: 5433）
POSTGRES_PORT=5432
